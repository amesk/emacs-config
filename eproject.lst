;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/amesk/.emacs.d/elpa/eproject-0.4")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 24)
        (left . 0)
        (width . 238)
        (height . 69)))

